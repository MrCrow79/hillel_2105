version: '3.8'

services:
  db:
    image: postgres
    environment:
      POSTGRES_DB: test_db
      POSTGRES_USER: test_user
      POSTGRES_PASSWORD: test_password
    ports:
      - "5432:5432"

  tests:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - /home/den/hillel/hillel_2105/allure-results/:/app/allure-results/
      - /home/den/hillel/hillel_2105/allure-report/:/app/allure-report/


