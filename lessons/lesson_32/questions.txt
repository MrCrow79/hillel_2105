- Які інструменти ви використовували для автоматизації тестування?
- Selenium/WebDriver. Які основні методи ви використовуєте?
- що слід автоматизувати, а що ні?
- опишіть архітектуру фреймворка для UI

- де можна зберігати expected results для тестів ?


- Що таке ООП? Як ви використовуєте принципи ООП в автоматизації?
- classmethod vs static method
- для чого використовувавти


- що таке фікстури ?
- який scope буває у фістур ?
- що таке mark ? Як їх можна використовувати ?
- розкажіть про parametrize


- робота з exceptions
- загальна схема в CI/CD для автотестів
- які ваші дії якщо тести не запускаються в Jenkins, а локально запускаються

- Які основні методи HTTP запитів ви знаєте?
- Різниця між GET POST/PUT/PATCH DELETE
- Чи можна в POST послати query parameters. Чи можна в get послати body ?
- які основні статус коди знаете. В чому різниця ?

- Які є нюанси при роботі з файлами ?
- що таке менеджер контексту ? __enter__ , __exit__

- Чи є досвід роботи з БД ?
- Які є нюанси ?
- sql запити. Join, group by | select ..  from .. join .. where .. group by .. order by .. having ... limit(top)

- Що таке docker.
- Де використовуеться. Для чого? docker exec -it image_name sh

- Що таке гіт.
- Яка різниця між Pull, Push? featch
- Для чого робити гілки ?

- Чи працювали з Алюр.
- Які є плючи і мінуси ?

Практика:
Як можна протестувати, що в таблиці users нема юзерів з однаковим прізвищем ?

select count(surname) as c_surname, surname from users group by surname having  c_surname > 1
select surmane from users # засунути в set і порівняти з масивом початковим)


Напишіть тест який перевіряє, що запит на  get users повертаеться мінімум 5 юзерів ?

def


напишіть функцію яка перевіряє чи є число простим

напишіть функцію яка приймає словник, а повертає список унікальних значень для ключів цього словника.
Для простоти словним в якості значення може мати тільки int та str

відсортуейте список від найбільшого до найменшого( список містить тільки числа)
відсортуйте список від найбільшо до найменгого чиста, всі None винесіть вперед)списк містить числа та None)